# Chat-app
 This is a chat application built using React with Firebase as the backend for authentication and secure data storage. It allows users to register, log in, and chat. Firebase handles user authentication and stores messages securely in the database. The project also uses Zustand for state management and other helpful node modules to enhance the app's functionality.
